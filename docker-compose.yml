services:
  pipeline:
    build: .
    container_name: email-analysis-pipeline
    env_file:
      - .env
    volumes:
      - ./ARTIFACTS:/app/ARTIFACTS
      - ./llm_calls.jsonl:/app/llm_calls.jsonl
